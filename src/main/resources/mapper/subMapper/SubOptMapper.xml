<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shengxi.system.model.mapper.sub.SubOptMapper">

    <sql id="key">
        <trim suffixOverrides=",">

            <if test="id!=null">
                id,
            </if>

            <if test="stuNo!=null">
                stu_no,
            </if>

            <if test="subId!=null">
                sub_id,
            </if>

            <if test="subName!=null">
                sub_name,
            </if>

            <if test="status!=null">
                status,
            </if>

            <if test="instituteRemark!=null">
                institute_remark,
            </if>

            <if test="academicRemark!=null">
                academic_remark,
            </if>

        </trim>
    </sql>

    <sql id="value">
        <trim suffixOverrides=",">

            <if test="id!=null">
                #{id},
            </if>

            <if test="stuNo!=null">
                #{stuNo},
            </if>

            <if test="subId!=null">
                #{subId},
            </if>

            <if test="subName!=null">
                #{subName},
            </if>

            <if test="status!=null">
                #{status},
            </if>

            <if test="instituteRemark!=null">
                #{instituteRemark},
            </if>

            <if test="academicRemark!=null">
                #{academicRemark},
            </if>

        </trim>
    </sql>

    <insert id="insereSubOptEntity" parameterType="com.shengxi.system.entites.subEntity.SubOptEntity">
        insert into sub_opt(<include refid="key"/>) values(<include refid="value"/>);
    </insert>

    <select id="selectSubOptEntityList"
            resultType="com.shengxi.system.entites.subEntity.SubOptEntity">
        select * from sub_opt
        <where>

        </where>
    </select>

</mapper>